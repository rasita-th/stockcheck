# Source registry

`data/source_registry.json` contains company aliases, configured primary-source domains and optional IR feeds. Ambiguous ticker symbols are never matched from the ticker token alone. Entries may be disabled when public-news matching would be unsafe or irrelevant.
