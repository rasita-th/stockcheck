# Golden contracts

Golden JSON files represent the minimum legacy fields that must remain readable during additive data migrations. PR validation fails when a current payload removes one of these fields.
